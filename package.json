{
  "name": "@rain/minimal-omni-public-preview",
  "version": "0.2.0-preview.1",
  "private": true,
  "type": "module",
  "description": "Clean public-preview staging for DeepSeek Harness Minimal Omni",
  "scripts": { "test": "node --test tests/*.test.mjs" }
}
