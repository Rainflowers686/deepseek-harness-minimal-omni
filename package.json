{
  "name": "deepseek-harness-minimal-omni-github",
  "version": "0.1.0-preview.1",
  "description": "A least-privilege GitHub capability for Minimal Omni, using the authenticated GitHub CLI without arbitrary shell execution.",
  "type": "module",
  "bin": {
    "dsh-github": "src/cli.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/gh-client.mjs && node --check src/cli.mjs && node --check scripts/secret-scan.mjs && node --check scripts/verify-install.mjs",
    "secret-scan": "node scripts/secret-scan.mjs",
    "verify-install": "node scripts/verify-install.mjs"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Rainflowers686/deepseek-harness-minimal-omni.git"
  },
  "publishConfig": {
    "access": "public"
  }
}
