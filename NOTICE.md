DeepSeek Harness Minimal Omni is an independent community project and is not
an official DeepSeek product. The official DeepSeek Harness runtime is loaded
from the user's DSH installation. This package uses its public plugin and
bundle interfaces; it does not redistribute the DSH runtime.

Keep the upstream and optional browser/document notices required by the
installation that supplies those components.
