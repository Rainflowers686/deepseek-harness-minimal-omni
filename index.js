// The runtime is mounted from cordis.patch.yml. This entry keeps the package
// a normal ESM distribution package for DSH's bundle resolver and subpath
// exports; it intentionally adds no model-facing behavior by itself.
export const name = 'minimal-omni'
